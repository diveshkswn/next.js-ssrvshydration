import React from 'react';
// eslint-disable-next-line import/no-named-as-default
import ToggleComponent from '../../utils/useToggle/ToggleComponent';

export default function Toggle(props) {
  return (
    <ToggleComponent />
  );
}
